const { hasStorage, listEvents, listSubmissions } = require("./_lib/storage.js");
function today(){return new Date().toISOString().slice(0,10)}
module.exports = async function handler(req,res){try{const dateFrom=req.query.date_from||process.env.CAMPAIGN_START_DATE||"2026-06-16";const dateTo=req.query.date_to||today();const events=await listEvents(dateFrom,dateTo);const submissions=await listSubmissions(dateFrom,dateTo);res.status(200).json({ok:true,storage_ready:hasStorage(),date_from:dateFrom,date_to:dateTo,event_count:events.length,submission_count:submissions.length,last_event:events[events.length-1]||null,last_submission:submissions[submissions.length-1]||null,required_env:{GITHUB_TOKEN:hasStorage(),GITHUB_OWNER:!!process.env.GITHUB_OWNER,GITHUB_REPO:!!process.env.GITHUB_REPO,GITHUB_BRANCH:!!process.env.GITHUB_BRANCH,GITHUB_DATA_PATH:!!process.env.GITHUB_DATA_PATH,CAMPAIGN_START_DATE:!!process.env.CAMPAIGN_START_DATE}})}catch(error){res.status(500).json({ok:false,error:"debug_failed"})}};
